[package]
name = "syon-parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "syon_parser"
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
pest = "2"
pest_derive = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
