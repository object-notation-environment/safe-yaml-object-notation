[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "syon-python"
requires-python = ">=3.8"
description = "Python bindings for the SYON parser"
version = "0.1.0"

[tool.maturin]
module-name = "syon_python"
manifest-path = "Cargo.toml"
